include "application.base.conf"

akka {
  loglevel = INFO
  http {
    verbose-error-messages = off
    server {
      idle-timeout = 90s
      request-timeout = 60s
      remote-address-header = on
      # The value of the `Server` header to produce.
      # Set to the empty string to disable rendering of the server header.
      server-header = ubirch-avatar-service
    }
  }
}

ubirchAvatarService {
  //  interface = "localhost"
  interface = "0.0.0.0"
  port = 8080

  es {
    host = search-ubirch-storage-service-oexhx3affoffn3tc4fe3wie4f4.us-east-1.es.amazonaws.com
  }
  aws {
    localmode = false
  }
}